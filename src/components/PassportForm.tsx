import React from 'react';
import { FormContainer } from './FormContainer';
import { TextField } from '@mui/material';

export const PassportForm = () => {
    return (
        <FormContainer title="Please ...">
            <TextField variant="outlined" />
        </FormContainer>
    );
};
