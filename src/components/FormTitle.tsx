import { styled } from '@mui/material';

export const FormTitle = styled('h2', {
    name: 'FormTitle',
})({
    color: '#3e3e3e',
    fontSize: '1.5rem',
    fontWeight: 600,
    textTransform: 'uppercase',
    width: '100%',
    margin: 0,
});
